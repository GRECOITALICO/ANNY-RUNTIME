schema_version: "1.0"
record_type: KIRA_PR38_UNIVERSAL_CAPABILITY_DELTA_RECONCILIATION
status: PROVISIONAL_RECONCILIATION
date: "2026-09-23"
owner: KIRA / ENGINEERING
parent_authority: ANNY / DTO

purpose: >
  Reconcile the existing Runtime hardening PR with the Universal Capability
  implementation frontier so Codex does not repeat work already implemented
  on the draft branch. This is a static source comparison; it does not
  authorize merge, execution, certification or production.

comparison:
  main_commit: "4577a3b67199b863dc556729d5095710231be4d2"
  pr_commit: "6ca2f6da51a6817586c45f53ecc54a7da5f53d2e"
  pr: "ANNY-RUNTIME#38"
  pr_state: "DRAFT / OPEN / UNMERGED"

already_addressed_or_partially_addressed_by_pr38:
  - area: "Canonical Runtime admission / direct-route denial"
    evidence:
      - "tests/test_authorization_path_batch3.py"
      - "runtime/execution/manager.py"
      - "runtime/execution/worker.py"
    interpretation: >
      The PR establishes a Runtime-local canonical admission transition and
      rejects direct executor/tool invocation without that admission.
    universal_capability_status: "PARTIAL_ALIGNMENT"
    note: "This does not establish the Universal Capability semantic catalog as the canonical semantic authority."

  - area: "Frontier executor provenance/fail-closed boundary"
    evidence:
      - "runtime/orchestration/frontier.py"
      - "runtime/execution/worker.py"
    universal_capability_status: "SUPPORTING_HARDENING"

  - area: "Filesystem containment / workspace resource boundary"
    evidence:
      - "runtime/security/path_containment.py"
      - "tests/test_path_containment_batch2.py"
    universal_capability_status: "SUPPORTING_HARDENING"

  - area: "Fabric trust / governed-write fail-closed boundary"
    evidence:
      - "runtime/fabric/governed_write.py"
      - "tests/test_fabric_trust_and_governed_write.py"
    universal_capability_status: "SUPPORTING_HARDENING"

  - area: "Release/update/sync lifecycle truthfulness"
    evidence:
      - "runtime/sync/service.py"
      - "runtime/sync/verifier.py"
      - "docs/UPDATE-SYNC-LIFECYCLE-STATUS-001.md"
    universal_capability_status: "SUPPORTING_HARDENING"

still_open_for_universal_capability:
  - id: UC-DELTA-001
    area: "Canonical semantic binding"
    issue_refs: ["ANNY-RUNTIME#41", "ENGINEERING#21", "ENGINEERING#23"]
    evidence: >
      Orchestrator.plan_task() still resolves capability semantics against its
      Runtime-local CapabilityRegistry and implementation profiles. The PR's
      canonical admission does not make a globally approved Universal
      Capability identifier/binding the semantic source of truth.
    required_next: "Explicit approved Universal Capability -> Runtime binding contract."

  - id: UC-DELTA-002
    area: "ModelCapabilityBinding authorization gate"
    issue_refs: ["ANNY-RUNTIME#41"]
    evidence: >
      ModelRegistry exposes get_bindings_for_capability(), but
      LocalIntelligenceLayer.assess_capability() evaluates availability,
      capability support, certification, benchmark evidence, resource fit and
      policy fit without consuming binding authorization as a mandatory
      candidate gate.
    required_next: "Consume binding authorization before candidate eligibility."

  - id: UC-DELTA-003
    area: "Contextual realization semantics"
    issue_refs: ["ANNY-RUNTIME#39"]
    evidence: >
      Orchestrator.plan_task() still branches on CapabilityDefinition.inference_required
      and supplies a fixed quality_required=0.85 to capability assessment.
    required_next: "Approved typed realization/inference policy and contextual gate."

  - id: UC-DELTA-004
    area: "Evidence-confidence gate"
    issue_refs: ["ANNY-RUNTIME#40", "ENGINEERING#48"]
    evidence: >
      LocalIntelligenceLayer computes confidence but does not reject a candidate
      for insufficient evidence confidence before quality ranking.
    required_next: "Policy-driven minimum_evidence_confidence gate with explicit UNKNOWN semantics."

  - id: UC-DELTA-005
    area: "Certification provenance gate"
    issue_refs: ["ANNY-RUNTIME#26", "ANNY-RUNTIME#18", "ANNY-RUNTIME#24"]
    evidence: >
      BenchmarkRunner.build_scorecard() can still derive CERTIFIED from mean score
      at the configured threshold without the complete Universal Capability
      provenance/evidence tuple.
    required_next: "Separate measurement from certification and require complete provenance."

  - id: UC-DELTA-006
    area: "Universal semantic ABI"
    issue_refs: ["ENGINEERING#24", "ENGINEERING#46"]
    evidence: >
      Existing Runtime contracts continue to expose Runtime-local capability IDs
      directly to orchestration and model registries.
    required_next: "Approved ABI that decouples semantic capability identity from Runtime internals."

  - id: UC-DELTA-007
    area: "ANNY mediation / delegate-first"
    issue_refs: ["ENGINEERING#46", "UC-IMPL-013..025"]
    evidence: >
      PR #38 hardens Runtime admission but does not implement the proposed
      Universal Capability mediation loop, Work Graph binding, or ANNY_SELF
      exception semantics.
    required_next: "Implement only after ANNY approves MD-03 and related decisions."

  - id: UC-DELTA-008
    area: "Canonicalization"
    issue_refs: ["ENGINEERING#21", "ENGINEERING#50"]
    evidence: >
      The 1,560-leaf source remains a discovery corpus; PR #38 does not freeze
      canonical IDs or reconcile the full semantic catalog.
    required_next: "ANNY semantic review followed by controlled canonicalization/migration."

deduplication_conclusion:
  statement: >
    PR #38 should be treated as a prerequisite Runtime hardening layer, not as
    completion of Universal Capability integration.
  codex_reuse_rule:
    - "Do not recreate the canonical admission path already present in PR #38."
    - "Extend the existing admission/evaluation architecture only where the approved Universal Capability contract requires it."
    - "Keep PR #38 unmerged and Runtime main unchanged until ANNY chooses merge/implementation gates."
  implementation_frontier_order:
    - "First: preserve and consume existing canonical admission."
    - "Second: add approved semantic binding + ModelCapabilityBinding gate."
    - "Third: add contextual realization + evidence-confidence gates."
    - "Fourth: repair certification/provenance boundary."
    - "Fifth: integrate semantic ABI and three-plane mediation."
    - "Sixth: execute negative/positive integration tests."

non_decisions:
  canonical_ids_frozen: false
  pr38_merged: false
  live_runtime_verified: false
  certification: false
  production: false
  activation: false
  authority_expansion: false
  KIRA_P2: SEPARATE_AND_UNTOUCHED
